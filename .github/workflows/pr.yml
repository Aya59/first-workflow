name: pr filter demo
run-name: ${{ github.actor }} is filter pr demo 🚀
on:
  pull_request:
    # Sequence of patterns matched against refs/heads
    branches:
      - aya
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu
    steps:
      - run: echo "🎉 The job was by a ${{ github.event_name }} event."
      
